on:
  workflow_dispatch: 
jobs:
  this:
    runs-on: macos-latest
    steps:
      - run: whoami
      - run: echo "root:1" | sudo chpasswd
      - run: echo "runner:1" | sudo chpasswd
      - run: brew install tailscale
      - run: sudo tailscaled &
      - run: sudo tailscale up
      - run: sleep 66666
