on:
  workflow_dispatch: 
jobs:
  this:
    runs-on: macos-latest
    steps:
      - run: brew install tailscale
      - run: sudo tailscaled &
      - run: sudo tailscale up
      - run: sudo sysadminctl -addUser myuser -password newpassword -admin
      - run: sudo systemsetup -setremotelogin on
      - run: sleep 60000
